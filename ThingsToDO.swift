//
//  ThingsToDO.swift
//  weatherApp
//
//  Created by PS Shortcut on 25/08/2018.
//  Copyright © 2018 PS Shortcut. All rights reserved.
//

import Foundation

class ThingsToDo{
    
    // 1) Save the selected LocationID to AppDelegate. With this way when the user loads the app.                           - DONE
    //    it remembers the previous location that he ended the app and loads it at the first ViewController
    // 2) Check if the app loads for first time. If yes then segue to LocationsStoredViewController to add a location       - DONE
    
    
    // 4) ---------TIME ISSUE---------------
    //      a) Check if the user time and the time zone AND THEN load the relative UIScrollView
    //      b) Check when he downloaded last time and DOWNLOAD AGAIN the data
    
    // 5) Solution to problem with timezones is writen on to of ViewController and code at MyPlayground
    // 6) start implementing the Network part for parsing the rest of the data and saving to core data for VIEWCONTROLLER UI
    //    Make everything in small part so you are more efficient
    // 7) Time label in LocationStoredTableViewController solution is on cellTimUpdate project. Leave it for the end
    
    
    // ------------------ UPDATE ----------------------
    
    // Just parse the day prediction To finish the project
    
    // 1) Change the Storyboard
        //------- a) LocationStoredViewController
        //------- b) split on add an didselectrow
    
    // 2) 
    
}
